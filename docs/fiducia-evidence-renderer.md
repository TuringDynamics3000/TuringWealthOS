# Fiducia Evidence Renderer

Purpose:
Render immutable EvidencePack objects into regulator-grade artefacts.

Outputs:
- Canonical JSON (machine-verifiable)
- Markdown (human-readable, PDF-ready)

Properties:
- Deterministic
- Side-effect free
- Integrity-checked
- Replay-safe

Rendering NEVER mutates evidence.
