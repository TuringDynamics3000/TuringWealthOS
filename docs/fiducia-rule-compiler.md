# Fiducia Rule Compiler

Flow:
Mandate DSL ? AST ? Deterministic Authority Function

Properties:
- No side effects
- No clocks
- No randomness
- Replay-safe
- Reason-code emitting

The compiled function is the ONLY place rules are executed.
